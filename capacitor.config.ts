// Capacitor configuration file for Android and iOS builds
const config = {
  appId: 'com.worldradio.translator',
  appName: 'WorldRadioTranslator',
  webDir: 'dist',
  bundledWebRuntime: false,
  server: {
    androidScheme: 'https',
    cleartext: true
  }
};

export default config;
